# =============================================================================
# These instructions are written for Linux environments.
# On Windows, use WSL2 (Ubuntu 22.04+) — native Windows has hard path-length
# limits (260 chars) that cause pip install failures for several packages here.
# On macOS, use the standard macOS terminal (no WSL needed).
# =============================================================================

# PyTorch CUDA index — lets pip resolve the torch/torchvision/torchaudio CUDA
# wheels automatically without a separate manual install step.
# macOS users: remove this line (MPS wheels are on PyPI directly).
--extra-index-url https://download.pytorch.org/whl/cu128

# Core data / visualization stack
pandas
numpy
matplotlib
seaborn
scikit-learn

# Notebook kernel
jupyter
ipykernel

# PatchTST / Hugging Face training
# Pinned as a matched trio — torch + torchvision + torchaudio share a C++ ABI
# and must be kept at compatible versions together.
#
# macOS users: remove the --extra-index-url line above and replace these three
# lines with unpinned: torch torchvision torchaudio  (MPS backend, no CUDA).
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0
transformers
accelerate
tensorboard
huggingface-hub

# LoRA support — kept for parity with the IBM/granite reference notebook.
# Not used by the from-scratch PatchTST classifier.
peft

# IBM Granite TSFM baseline — uncomment once PyTorch is installed.
# Works cleanly on Linux/WSL2 and macOS. Disabled by default because
# the repo contains long filenames that fail on native Windows.
# granite-tsfm[notebooks] @ git+https://github.com/ibm-granite/granite-tsfm.git@v0.3.3

# Optional quantization — Linux/WSL2 with CUDA only.
# bitsandbytes
